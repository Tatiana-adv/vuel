import { createApp } from 'vue'
import './style.css' // Importa o CSS principal
import App from './App.vue'

// Cria a instância da aplicação Vue e monta no elemento com id="app"
createApp(App).mount('#app')
